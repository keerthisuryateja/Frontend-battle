# GitHub Pages Deployment Bundle

This folder contains only the files that matter for a GitHub Pages deployment.

- `.github/workflows/deploy.yml` for CI deployment
- `.gitignore` for keeping the repo clean
- `vite.config.js` base path set for safe relative asset loading
- `dist/` is generated by `npm run build` and should not be committed

Use the root app source as the canonical codebase.